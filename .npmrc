auto-install-peers=false
strict-peer-dependencies=false
# Don't auto-install before `pnpm run` — deps are managed explicitly and installed
# in the Docker image. Avoids a crash-loop where `pnpm run dev` tries to reinstall
# on container start and aborts on the modules-purge prompt (no TTY).
verify-deps-before-run=false
