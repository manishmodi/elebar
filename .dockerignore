# Keep the build context small and never bake host junk / secrets / PII into the image.
.git
**/node_modules
node_modules
**/dist
dist
.turbo
.vercel
*.log

# secrets / local env
.env
.env.*
!.env.example

# production data dumps (PII + password hashes)
elebhar_dump.sql
exports
*.tar.gz
*.dump
